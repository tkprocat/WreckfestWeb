--- a/vendor/redberry/mcp-client-laravel/src/Core/Transporters/HttpTransporter.php
+++ b/vendor/redberry/mcp-client-laravel/src/Core/Transporters/HttpTransporter.php
@@ -52,7 +52,7 @@ class HttpTransporter implements Transporter
         $payload = $this->preparePayload($action, $params);

         try {
-            $response = $this->client->request('POST', $action, [
+            $response = $this->client->request('POST', '', [
                 'json' => $payload,
                 'timeout' => $this->config['timeout'] ?? 30,
                 'headers' => [
